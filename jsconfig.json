// tsconfig.json (or jsconfig.json)

{
  "compilerOptions": {
    "baseUrl": ".",              // project root
    "paths": {
      "@/*": ["src/*"]           // <-- note both patterns end with /* 
    }
  }
}
